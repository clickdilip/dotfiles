- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.config/karabiner: karabiner
    ~/.zshrc: zshrc
    ~/.config/fish: fish

- create:
    - ~/downloads
    - ~/.vim/undo-history
    - ~/.config/karabiner
    - ~/.config/fish

- shell:
  - [git submodule update --init --recursive, Installing submodules]
